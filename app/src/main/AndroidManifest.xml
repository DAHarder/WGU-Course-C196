<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.c196projectdanadams">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.C196ProjectDanAdams">
        <activity android:name=".view.AssessmentEdit"
            android:label="Assessment"
            android:parentActivityName="view.CourseEditAssessmentList"/>
        <activity android:name=".view.CourseEditAssessmentList"
            android:label="Course and Assessments"
            android:parentActivityName="view.TermEditCourseList"/>
        <activity android:name=".view.TermEditCourseList"
            android:label="Term and Courses"
            android:parentActivityName="view.TermList"/>
        <activity android:name=".view.TermList"
            android:label="Terms"
            android:parentActivityName="view.MainActivity"/>
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>